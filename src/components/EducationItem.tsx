interface Props {
    year: number,
    duration: string,
    university: string,
    program: string,
    details: string,
}

const WorkItem = ({year, duration, university, program, details}: Props) => {
